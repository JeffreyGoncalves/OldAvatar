<UserControl x:Class="LecturerTrainer.View.DrawingSheetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             
             Width="640" Height="480">
    <Grid Margin="{DynamicResource Padding5x5x5x5}">
        <MediaElement Name="ReplayAudio" LoadedBehavior="Manual"/>
        <MediaElement Name="ReplayVideo" LoadedBehavior="Manual" ScrubbingEnabled="True"/>
        <Canvas Name="CanvasFeedback" Panel.ZIndex="3"/>
        <Image Name="MyImage"/>
        <WindowsFormsHost Name="MyHost" />
    </Grid>
</UserControl>